# ---- Core web app (lightweight — dashboard + demo data work with just these) ----
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.0.0

# ---- AI pipeline (heavier; needed for live detection) ----
ultralytics>=8.1.0
opencv-python>=4.8.0
easyocr>=1.7.0
numpy>=1.24.0
imageio[ffmpeg]>=2.31.0   # ships the ffmpeg binary used to read GPS from video metadata

# ---- GPU NOTE ----
# ultralytics/easyocr pull in torch. For your NVIDIA GPU install the CUDA build
# FIRST so inference uses the GPU, e.g. (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt
