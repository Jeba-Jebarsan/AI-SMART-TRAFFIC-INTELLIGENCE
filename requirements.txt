# ---- Core web app (lightweight — dashboard + demo data work with just these) ----
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.0.0

# ---- AI pipeline (heavier; needed only to Analyze real videos) ----
ultralytics>=8.1.0
opencv-python>=4.8.0
easyocr>=1.7.0
numpy>=1.24.0
imageio[ffmpeg]>=2.31.0   # browser-friendly H.264 annotated output
yt-dlp>=2024.1.0          # analyze from YouTube / stream URLs

# ---- GPU NOTE ----
# ultralytics/easyocr pull in torch. For your NVIDIA GPU install the CUDA build
# FIRST so inference uses the GPU, e.g. (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt
